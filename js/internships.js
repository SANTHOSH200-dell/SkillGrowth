let all=[];async function load(){try{all=await api("/api/internships/");render(all);const m=await api("/api/internships/my");internshipCount.textContent=all.length;myApplicationCount.textContent=m.length;applicationGrid.innerHTML=m.length?m.map(a=>`<div class="item"><b>${a.internship.title}</b><p>${a.internship.company} · ${a.status}</p></div>`).join(""):"No applications yet."}catch(e){internshipGrid.textContent=e.message}}function render(x){internshipGrid.innerHTML=x.map(i=>`<div class="item"><span class="tag">${i.mode}</span><h3>${i.title}</h3><b>${i.company}</b><p>${i.description}</p><small>${i.location} · ${i.stipend}</small><br><button class="btn btn-primary" onclick="applyI(${i.id})">Apply</button></div>`).join("")}async function applyI(id){try{alert((await api("/api/internships/apply",{method:"POST",body:JSON.stringify({internship_id:id})})).message);load()}catch(e){alert(e.message)}}refreshInternships.onclick=load;internshipSearch.oninput=e=>render(all.filter(i=>(i.title+i.company+i.description).toLowerCase().includes(e.target.value.toLowerCase())));load();