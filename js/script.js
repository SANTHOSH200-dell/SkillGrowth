const API_BASE="http://127.0.0.1:8000";document.addEventListener("DOMContentLoaded",()=>{const u=JSON.parse(localStorage.getItem("skillgrowth_current_user")||"null");if(u){document.querySelectorAll("#userName").forEach(x=>x.textContent=u.full_name||"Student");document.querySelectorAll("#userEmail").forEach(x=>x.textContent=u.email||"");document.querySelectorAll("#welcomeName").forEach(x=>x.textContent=(u.full_name||"Student").split(" ")[0]);document.querySelectorAll("#userAvatar").forEach(x=>x.textContent=(u.full_name||"TS").split(" ").map(v=>v[0]).join("").slice(0,2).toUpperCase())}document.getElementById("logoutBtn")?.addEventListener("click",()=>{localStorage.clear();location.href="login.html"});document.getElementById("mobileMenuBtn")?.addEventListener("click",()=>document.getElementById("sidebar")?.classList.toggle("open"))});async function api(path,opt={}){const h={"Content-Type":"application/json",...(opt.headers||{})},t=localStorage.getItem("skillgrowth_token");if(t)h.Authorization=`Bearer ${t}`;const r=await fetch(API_BASE+path,{...opt,headers:h}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.detail||"Request failed");return d}