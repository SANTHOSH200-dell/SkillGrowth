async function load(){try{let x=await api("/api/projects/");if(!x.length){await api("/api/projects/seed",{method:"POST"});x=await api("/api/projects/")}projectGrid.innerHTML=x.map(p=>`<div class="item"><span class="tag">${p.level}</span><h3>${p.title}</h3><p>${p.description}</p><button class="btn btn-primary" onclick="startP(${p.id})">Start Project</button></div>`).join("")}catch(e){projectGrid.textContent=e.message}}async function startP(id){try{await api("/api/projects/start",{method:"POST",body:JSON.stringify({project_id:id})});alert("Project started")}catch(e){alert(e.message)}}seedProjectsBtn.onclick=()=>load();load();